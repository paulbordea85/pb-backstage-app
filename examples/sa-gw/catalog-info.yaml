---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: sa-gateway
  # This is an extra long description with Markdown
  description: |
    sa-gateway
  annotations:
    backstage.io/kubernetes-id: sa-gateway
spec:
  type: service
  lifecycle: development
  owner: administrators
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: sp-gateway
  # This is an extra long description with Markdown
  description: |
    sp-gateway
  annotations:
    backstage.io/kubernetes-id: sp-gateway
spec:
  type: service
  lifecycle: development
  owner: administrators
---
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ei-gateway
  # This is an extra long description with Markdown
  description: |
    ei-gateway
  annotations:
    backstage.io/kubernetes-id: ei-gateway
spec:
  type: service
  lifecycle: development
  owner: administrators
